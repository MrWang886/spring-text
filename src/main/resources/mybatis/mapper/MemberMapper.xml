<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.milotnt.mapper.MemberMapper">

    <select id="findAll" resultType="member">
        SELECT *
        FROM member
    </select>

    <insert id="insertMember" parameterType="member">
        INSERT INTO member (member_account, member_name, member_gender, member_age, member_height,
                            member_weight, member_phone, card_time, card_class,
                            card_next_class)
        VALUES (#{memberAccount}, #{memberName}, #{memberGender}, #{memberAge}, #{memberHeight},
                #{memberWeight}, #{memberPhone}, #{cardTime}, #{cardClass}, #{cardNextClass})
    </insert>

    <update id="updateMemberByMemberAccount" parameterType="member">
        UPDATE member
        SET member_account  = #{memberAccount},
            member_password = #{memberPassword},
            member_name     = #{memberName},
            member_gender   = #{memberGender},
            member_age      = #{memberAge},
            member_height   = #{memberHeight},
            member_weight   = #{memberWeight},
            member_phone    = #{memberPhone},
            card_class      = #{cardClass},
            card_next_class = #{cardNextClass}
        WHERE member_account = #{memberAccount}
    </update>

    <select id="selectByAccountAndPassword" resultType="member">
        SELECT *
        FROM member
        WHERE member_account = #{memberAccount}
          AND member_password = #{memberPassword}
    </select>

    <delete id="deleteByMemberAccount" parameterType="java.lang.Integer">
        DELETE
        FROM member
        WHERE member_account = #{memberAccount}
    </delete>

    <select id="selectTotalCount" resultType="java.lang.Integer">
        SELECT count(*)
        FROM member
    </select>

    <select id="selectByMemberAccount" parameterType="java.lang.Integer" resultType="member">
        SELECT *
        FROM member
        WHERE member_account = #{memberAccount}
    </select>

    <!-- 插入打卡记录 -->
    <insert id="insertCheckin" parameterType="map">
        INSERT INTO member_checkin(member_account, checkin_date)
        VALUES (#{memberAccount}, #{checkinDate})
        ON DUPLICATE KEY UPDATE checkin_date = checkin_date
    </insert>

    <!-- 查询会员的打卡天数 -->
    <select id="selectCheckinCountByMemberAccount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM member_checkin
        WHERE member_account = #{memberAccount}
    </select>

    <!-- 查询所有会员的打卡天数（用于排行榜） -->
    <select id="selectCheckinRanking" resultType="map">
        SELECT m.member_name AS memberName, COUNT(c.checkin_id) AS checkinCount
        FROM member m
        LEFT JOIN member_checkin c ON m.member_account = c.member_account
        GROUP BY m.member_account
        ORDER BY checkinCount DESC
    </select>

    <!-- 在MemberMapper.xml中添加连续打卡天数的查询（需根据实际需求编写SQL） -->
    <select id="selectContinuousCheckinDays" resultType="integer">
        <!-- 示例SQL，实际需要计算连续天数 -->
        SELECT COUNT(*) 
        FROM member_checkin 
        WHERE member_account = #{memberAccount}
    </select>

</mapper>